server:
  port: 8888
  
spring:
  #Spring Cloud Config Server Repository
  cloud:
    config:
      server:
        git:
          uri: https://github.com/alexproject8/config-server2.git
          default-label: rama-config
          search-paths:
          - '*service'